# Core
pymupdf>=1.24.0                # PDF parsing (PyMuPDF / fitz)
chromadb>=0.5.0                # local vector store
sentence-transformers>=3.0.0  # local embeddings (all-MiniLM-L6-v2)
rank-bm25>=0.2.2               # BM25 keyword search for hybrid retrieval

# LLM orchestration
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0        # Ollama adapter (free, local)
requests>=2.32.0               # direct Ollama HTTP calls

# Data / config
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# UI
streamlit>=1.40.0

# Observability
structlog>=24.0.0
mlflow>=2.18.0

# Evaluation
ragas>=0.2.0                   # optional — if you want RAGAS metrics
pytest>=8.0.0
